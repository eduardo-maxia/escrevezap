<% content_for :title, t("pages.termos.title") %>

<div class="max-w-3xl mx-auto px-4 sm:px-6 py-16">
  <h1 class="text-3xl font-bold text-(--color-text) mb-8"><%= t("pages.termos.title") %></h1>
  <div class="prose prose-sm text-(--color-text-muted) space-y-4">
    <p><%= t("pages.termos.body") %></p>
  </div>
</div>
